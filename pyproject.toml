[project]
name = "robotics-isaac-learning"
version = "0.1.0"
description = "CLI-driven Isaac Lab learning experiments on Brev"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
